# Runtime uses Python standard library plus ttkbootstrap for the modern GUI theme.
# Recommended Python version: >=3.10
ttkbootstrap>=1.10
#
# Optional acceleration build:
#   py -m pip install -r requirements.txt
#   py setup_accel.py build_ext --inplace
Cython>=3.0
setuptools>=68
